# Project conventions

Run `make test` before every commit.
Also run `make verify` for the full suite.
Lint with `npm run lint`, and type-check with `npm run typecheck`.

The API handlers live in `src/app.py`.
Shared helpers are in `src/helpers/legacy.py`.
